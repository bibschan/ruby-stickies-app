
<div class="post-format">
	<h1> <%= @post.title %> </h1>
	<div>
		<%= @post.body %>
	</div>
</div>
<br>
<%= link_to "Edit Note", edit_post_path(@post) %>
<br>
<%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Are you sure?" } %>   